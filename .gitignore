# ─── Vivado projects (not tracked) ───────────────────────────────────────────
MDF FFT/
Serial_FFT_Vivado/
ip_repo/

# ─── Python virtual environment ──────────────────────────────────────────────
**/.venv/
**/venv/
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd

# ─── IDE / Tool configs ───────────────────────────────────────────────────────
**/.claude/
**/.gateflow/

# ─── Synthesis artifacts ──────────────────────────────────────────────────────
# Track .ys scripts and .log reports (small text), skip large netlists
**/synthesis/*.json
**/synthesis/*.edif
**/synthesis/synth_rtl/
**/synthesis/pnr.log

# ─── Simulation outputs ───────────────────────────────────────────────────────
**/*.csv
**/*.vcd
**/*.wdb
**/*.saif
**/fft_sim
**/fft_axi_sim
**/sim_axi
**/fft_*_sim
**/fft_*_sim_*
**/fft_sim_*
**/fft_axi_sim_*
**/*.vvp
**/hw_exponent.txt
**/hw_output.csv

# ─── Duplicate mem files (kept only under rom/) ───────────────────────────────
parallel_mdf_fft/twiddle_cos_eighth.mem
parallel_mdf_fft/twiddle_sin_eighth.mem

# ─── Serial FFT root-level mem duplicates (canonical copies in rom/) ─────────
serial_fft/cos.mem
serial_fft/sin.mem

# ─── Generated DSP-metric / co-simulation outputs (regenerable) ───────────────
results/

# ─── UVM generated / simulation artifacts (Vivado xsim) ───────────────────────
# Track only env/seq/tests/tb/scripts/docs + Makefile + README.
# (No inline comments below — .gitignore treats them as part of the pattern.)
UVM/xsim.dir/
UVM/work/
UVM/cov_db/
UVM/cov_html/
UVM/xsim.covdb
# NumPy reference vectors — regenerate with `make refs`
UVM/refs/
# captured scoreboard outputs — regenerate with `make equiv`
UVM/actuals/
UVM/*.wdb
UVM/*.jou
UVM/*.log
UVM/*.pb
UVM/*.backup.*
# regression / coverage / equivalence summary PNGs (regenerable)
UVM/*.png
# root-level ROM copies the Makefile stages at runtime
UVM/*.mem
# twiddle ROMs the Makefile copies from parallel_mdf_fft/rom
UVM/parallel_rom/
UVM/modelsim.ini

# ─── LaTeX thesis + tooling (out of scope for the code repo) ──────────────────
# The LaTeX sources stay local; only the compiled PDF is tracked under docs/.
thesis/
thesis_overleaf.zip
graphify/
graphify-out/

# ─── HLS: keep C/C++ sources, drop the regenerable Vitis HLS projects ─────────
hls/mdf_fft_hls/
hls/serial_fft_hls/
hls/mdf_xcheck/
hls/serial_xcheck/
hls/*.log

# ─── Working documents & scratch (regenerable / not portfolio material) ───────
*.docx
*.zip
kpi_summary/
parametric_study/build/
parametric_study/*.log
